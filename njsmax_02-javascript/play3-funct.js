
// options

function summarizeUser1(userName, userAge, userHasHobbies) { };

const summarizeUser2 = function (userName, userAge, userHasHobbies) { };

const summarizeUser3 = (userName, userAge, userHasHobbies) => { };

const add1 = (a, b) => {
    return a + b;
}

const add2 = (a, b) => a + b;

const addOneA = (a) => a + 1;
const addOneB = a => a + 1;

const addRandom = () => 1 + 2;